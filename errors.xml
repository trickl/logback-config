<?xml version="1.0" encoding="UTF-8"?>
<included>
  <appender name="ERRORS_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_PATH}/errors.log</file>
    <if condition='isDefined("NEW_RELIC")'>
      <then>
        <encoder class="com.newrelic.logging.logback.NewRelicEncoder"/>
      </then>
      <else>
        <encoder>
          <pattern>%d{HH:mm:ss.SSS} %yellow(%X{time}) %X{instructionId} %X{temporalSource} %highlight(%-5level) %cyan(%logger{15}) - %msg %n</pattern>          
        </encoder>
      </else>
    </if>

    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>
                ${LOG_PATH}/archived/errors_%d{dd-MM-yyyy}_%i.log
      </fileNamePattern>
      <maxFileSize>10MB</maxFileSize>
      <maxHistory>10</maxHistory>
      <totalSizeCap>100MB</totalSizeCap>
    </rollingPolicy>
  </appender>

  <if condition='isDefined("NEW_RELIC")'>
    <then>
      <appender name="NEW_RELIC_ERRORS_FILE" class="com.newrelic.logging.logback.NewRelicAsyncAppender">
        <appender-ref ref="ERRORS_FILE" />
      </appender>

      <logger level="ERROR">
        <appender-ref ref="NEW_RELIC_ERRORS_FILE" />
      </logger>
    </then>
    <else>  
      <logger level="ERROR">
        <appender-ref ref="ERRORS_FILE" />
      </logger>
    </else>
  </if>
</included>
